import * as React from 'react';

import s from './Footer.scss';

export const Footer = () => (
  <div className={s.footer}>
    {/* only here to add extra spacing on bottom in mobile, maybe later, content? */}
  </div>
);
